# jee-mocktests (deploy-ready)

This repository contains a deploy-ready IIT JEE mock-test platform (demo). It is configured to use a PostgreSQL database (recommended on Render or other hosts).

Follow the instructions in README_DEPLOY.md to deploy on Render.com.
